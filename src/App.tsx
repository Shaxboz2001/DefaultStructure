import React from "react";
import {
     useApi,
     useApiMutation,
     useApiMutationWithId,
} from "hooks/useApi/useApiHooks";
const App = () => {
     return <div></div>;
};

export default App;
